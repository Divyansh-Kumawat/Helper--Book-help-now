{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "both": "concurrently \"npm run server --prefix backend\" \"npm start --prefix frontend\""
  },
  "dependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "bcryptjs": "^3.0.2",
    "concurrently": "^9.2.0",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
